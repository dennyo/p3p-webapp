@{
    WebSecurity.InitializeDatabaseConnection("authenticationData", "UserProfile", "UserId", "UserName", autoCreateTables: true);

    App.Name = "Cyberdyne";

    App.Thumbnail = "/Photos/Thumbnail/";
    App.ViewFull = "/Photos/ViewFull/";

    App.DataBase = "dummyData";
    App.AuthDataBase = "authenticationData";

    //Emergency Measures

    /*var db = Database.Open(App.DataBase);
    db.Execute("DELETE FROM Photos WHERE PhotoId != 1")*/;
}